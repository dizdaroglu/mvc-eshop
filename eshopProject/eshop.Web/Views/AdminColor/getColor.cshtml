@model IEnumerable<eshop.EntitiesLayer.Models.Colors>

@{
    ViewBag.Title = "getColor";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Colors</h2>

<a href="/Admin/Index"><span class="fa fa-arrow-left  "></span></a>
<a href="/AdminColor/Create" style="float:right;"><span class="fa fa-plus-circle"></span></a>
<table class="table">
    <tr>
        
        <th>
            Code
        </th>
        <th>
            Name
        </th>
        <th>
            CreatedDate
        </th>
        <th>
            ModifiedDate
        </th>
        <th>
            RemovedDate
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModifiedDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RemovedDate)
        </td>
        <td>
            <a href="/AdminColor/Update/@item.ColorsId" class="btn btn-success" title="Güncelle"><span class="fa fa-pencil"></span></a>
            <a href="/AdminColor/Delete/@item.ColorsId" class="btn btn-danger" title="Sil"><span class="fa fa-trash"></span></a>
            <a href="/AdminColor/Details/@item.ColorsId" class="btn btn-info" title="Detay"><span class="fa fa-list"></span></a>
        </td>
    </tr>
}

</table>
